# Bank Campaigns Generator

Script retrieves data from the website with banking campaigns and then saves them in the Firebase database.

# Installation

Run `npm install`

change `.env.example` to `.env` and fill paramaters.

Run `npm run generate`
